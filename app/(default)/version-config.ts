/**
 * Landing Page Version Configuration
 * 
 * To switch between versions, change the 'activeVersion' value below.
 * Available versions: 'v1' | 'v2' | 'v3'
 * 
 * Example:
 *   export const activeVersion = 'v1';  // Use original landing page
 *   export const activeVersion = 'v2';  // Use new SpiderX landing page
 *   export const activeVersion = 'v3';  // Use Forest/Museum-core design system landing page
 */

export const activeVersion: 'v1' | 'v2' | 'v3' = 'v3';

